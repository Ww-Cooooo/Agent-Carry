# 路由使用说明

普通 Agent 只需执行 `BOOTSTRAP.md`。本说明供维护或排错时阅读。

- 根地图只包含分类元数据。
- 分类地图只包含能力摘要、触发条件、目标路径、状态、最低模型等级和确认门。
- 目标正文只在被选中后读取。
- 规则匹配优先，语义判断只在候选歧义或无候选回退时使用。
- 多能力任务允许少量组合，详见 `core/protocols/CONTEXT_ROUTING.md`。

新增路线时，必须同时登记组件关系和看板/升级影响；不要在正文内部写“先读我才能知道不该读我”的自门禁。
