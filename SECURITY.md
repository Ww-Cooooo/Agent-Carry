# 安全问题报告

AI Carry 会处理记忆、工作方式和本地个人资料。怀疑存在漏洞时，请不要把利用步骤、真实隐私或秘密凭据写进公开 Issue。

## 私密报告入口

请使用 GitHub 仓库的 [私密漏洞报告](https://github.com/Ww-Cooooo/Agent-Carry/security/advisories/new)。这个入口会把报告发送给仓库维护者，而不是公开展示。

如果该页面暂时没有显示报告按钮，请不要改用公开 Issue 粘贴漏洞细节。可以只新建一条不含技术细节、日志、个人数据或秘密原值的 Issue，说明“私密安全报告入口不可用”，等待维护者恢复入口。

## 报告中可以包含

- 受影响的 AI Carry 版本与来源；
- 使用的操作系统、宿主 Agent 和相关功能；
- 最小复现步骤；
- 可能造成的实际影响；
- 使用虚构占位符或脱敏材料得到的证据；
- 你已经采取的临时止损措施。

## 不要提交

- API 密钥、密码、令牌、Cookie、私钥、恢复码或登录态；
- 真实个人资料、私密记忆、完整实例包或未经脱敏的日志；
- 与漏洞无关的账号、仓库或设备信息；
- 为了证明问题而对他人账号、数据或系统进行的未授权测试。

如果漏洞涉及真实凭据，请先由凭据所有者在对应服务中撤销或轮换；不要把原值交给维护者验证。

## 维护范围

安全修正优先面向最新正式 Release。报告旧版本问题时，维护者可能先确认最新版本是否仍受影响。项目会核对报告、尽量缩小影响并决定修正与披露方式，但不承诺固定响应时间、漏洞奖金或绝对安全。

AI Carry 的通用接入以自然语言协议为基础。它可以用摘要、精确预览、原子写入和回滚防止内容漂移或半套写入，但不能从模型可提交的普通 JSON 中密码学证明消息角色；当前宿主必须真实展示需要用户决定的内容并等待回复。如果宿主本身被攻破或主动绕过协议，这超出通用母版能独立保证的范围。能够证明用户在场的宿主专用能力，只会作为单独审查的可选增强，不会被通用运行时伪装出来。

网页、邮件、附件和工具结果中出现诱导性文字，本身不一定构成 AI Carry 漏洞；如果这些内容实际绕过了任务、权限、接收方、秘密凭据或资源边界，请按安全问题报告。
