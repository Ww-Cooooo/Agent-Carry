# 参与 AI Carry

无论你是第一次使用 Agent、偶尔写一点代码，还是长期开发 Agent 工具，都可以参与。最有价值的反馈不是“再加一个功能”，而是把真实情况讲清楚：你当时想完成什么、哪里卡住了、希望最后得到什么结果。

## 先选择正确入口

| 你遇到的情况 | 建议入口 |
| --- | --- |
| 安装失败、看板打不开、动作结果不对 | 新建 Bug 报告 |
| 有一个真实场景目前做起来很麻烦 | 新建功能建议 |
| 怀疑存在安全漏洞或隐私泄漏 | 先阅读 [安全报告说明](SECURITY.md)，不要把细节写进公开 Issue |
| 想修改代码、文档、看板或协议 | 先阅读本页，再按需查看 [开发者定制指南](docs/developer-guide.md) |

普通反馈可以直接使用 GitHub 提供的表单。你不需要先懂项目架构，也不必先提出技术方案；把真实问题和期望结果写清楚即可。

## 报告问题时请提供

- 使用的 AI Carry 版本或下载来源；
- 操作系统，以及当时使用的宿主 Agent；
- 你原本想完成什么；
- 实际发生了什么，是否可以稳定复现；
- 已经尝试过哪些操作；
- 经过脱敏的截图或错误信息（如果确实有帮助）。

请先删除 API 密钥、密码、令牌、Cookie、私钥、恢复码、登录态、真实个人资料、私密记忆正文和本机私人路径。不要为了方便排查而上传整个个人实例。

## 提交修改

1. Fork 公开仓库，并在自己的分支中只处理一个边界清楚的问题。
2. 先根据 `core/maps/component-map.toml` 找到受影响组件；修改核心行为时遵守 `core/protocols/COMPONENT_CHANGE.md`。
3. 只运行与变化相关的检查。看板改动使用 `dashboard/README.md` 中的构建与离线检查；文档改动核对链接、事实和引用；Schema 或 TOML 改动至少验证解析和关键约束。
4. 在 Pull Request 中说明：真实问题、采用的解决方式、验证结果、仍然存在的限制。

不要提交 `node_modules`、本地缓存、个人实例数据、验收临时目录、秘密凭据或维护者私密流程。新增字体、图片、代码片段、依赖或其他第三方材料前，必须确认自己有权提供，并按 [开源合规与第三方来源](docs/open-source-compliance.md) 保留许可证和来源说明。

AI Carry 采用 Apache License 2.0。提交原创内容表示你有权按项目许可证提供该内容；第三方内容继续受其原许可证约束。

## 项目会怎样处理反馈

维护者会先判断问题是否可复现、是否符合 AI Carry 的定位，以及解决方式会不会让普通用户承担额外复杂度。被接受的建议也可能采用不同实现；没有真实需求支撑的重型流程、后台服务或宿主专用绑定不会仅为了“功能更多”而加入。
