# AI Carry 启动

启动阶段只做这些事，不提前读取协议、SOP、记忆或能力正文。

`Agent Carry` 是 `AI Carry` 的旧名，单独出现不算损坏。只有当前产品仍自称旧名、身份冲突或用户要求审查时，才走产品身份路线。

1. 在模型上下文外运行 `node dashboard/scripts/query-startup-capsule.mjs <AI Carry 根目录>`。它从严格 manifest/core 生成白名单启动信息并核对胶囊；原始 `assistant.toml`、`instance/manifest.toml`、显示名称、使命和自由文本不得进入上下文。胶囊缺失或陈旧时最多重建一次；真源有效但修复失败时返回 `startup-degraded`，继续对话和只读任务，持久变更前再定向修复。`startup-repair-required` 才表示真源无效：只暂停依赖身份的持久操作，报告影响与下一步，不把原清单送入模型绕过。
2. 用有效胶囊或降级投影确认模板／实例与方向。旧版缺学习政策时返回 `manual-only + migration_required=true`；正式真源非法或超限才失败。胶囊漂移不能拖死整个 Agent。
3. 固定入口会在模型上下文外严格解析胶囊给出的信号控制与信号地图，只把一个状态、至多一个短路线和唯一最早唤醒时间组成的白名单摘要返回给模型。信号更新事务的其他输出不是启动读取清单；启动不得自行打开原始信号 TOML，也不得因为看到时间投影、来源根、注册表、协议、Schema 或模板字段就打开对应文件或枚举目录。日期未到时不得读取时间索引；普通观察状态、全部日程、指标、状态卡和规则正文都不在启动时扫描。
4. 读取胶囊中固定 `root_map_ref` 指向的根地图，为当前请求选择一个根分类；只有确有组合需要时才选择少量多个分类。
5. 再读取所选分类的小地图，并只打开匹配目标。

准备读取 `instance/maps/domain-map.toml` 时，先在模型上下文外只检查文件字节数和有界 `[budget]` 头。文件超过硬线或 `overflow_state` 不为 `ok` 时，不得把整份地图送入上下文：带合法稳定 ID 的请求可做受验证的定向查找；模糊请求先用用户当前交流语言帮助缩小意图，并投影一条去重维护信号。正常小地图不因此加载额外正文。

所有面向用户的交流都遵守 `assistant.toml` 的极小 `interaction` 基线：默认用户可能不熟悉 Agent、编程、路径或内部术语；先给人能理解的结果与背景，再说明真正需要用户做的下一步，不能用功能名、路径、状态码或技术报告代替引导。任务完成、暂停等待决定或因限制交还用户时，`🧠` 使用回执与 `🌱` 学习回执不得成为回复结尾；最后用带 `👉` 的本地化“接下来”区块，从用户总体目标的未完成工作中明确推荐一项首选行动。刚完成的小步骤不等于主任务完成；只要仍有相关工作，就不能用“这件事无需确认”或“现在无需操作”代替真正下一步。需要用户作出实质选择时，复用 Agent 已知信息，给出带后果的完整选项、能成立时的推荐和“我不确定，请帮我判断”的入口。任务创建、移动或交付用户可见文件后，如果长期保留与携带意图仍不明确，也属于这个检查点；不得让用户重新提供 Agent 已知路径。三种交流方式只能调整篇幅和节奏，不能删掉决定所需信息。只有多步骤、技术性、风险性、长期性或用户不确定的选择，才从 `domain-lifecycle` 命中 `user-decision-guidance` 并加载完整协议。

如果已验证启动胶囊的 `state = "template"`：

- 立即从 `domain-lifecycle` 选择 `first-run-guidance`，读取 `core/guides/instantiation-guide.md` 的“安装成功后的第一条回复”与当前所需段落。不得用安装报告、版本／路径清单或一句没有解释的交流方式问题结束对话，也不得因无法证明模型等级而跳过欢迎、首次选择或确定性创建。
- 第一条面向用户的话必须先邀请创建自己的助手；当前任务包含安装时，同时说明看板是否已经在用户可见窗口中打开、怎样从首页右侧的“第一次使用”卡片进入“创建我的助手”，以及不看或打不开看板时可以直接在当前聊天继续。
- 在同一条回复中完整解释三种引导路线适合谁、当前 Agent 会怎样配合，再只询问这一个选择；用户不知道时允许直接说职业、困难和目标，由 Agent 帮助判断。三种路线只是可调整的交流方式，不是能力等级。
- 安装入口验证已经结束、准备发送这条回复时，重新读取 `core/guides/first-use-execution-gates.md` 的“A. 安装完成后的回复门”；不能因为较早读过实例化指南就跳过临门检查。
- 用户回答后，再询问选择“通用个人助手”“具体领域专属助手”还是“先帮我判断”。说明专业工作通常更适合领域专属助手；“先帮我判断”不是第三种正式方向；方向一旦实例化便永久锁定，仍可复制模板创建其他实例。
- 看板生成的请求已经带有合法选择时不要重复询问；其中的“生成创建指令”只是在组织请求，不代表已经实例化。保留已有选择，并在正式访谈前从 `domain-lifecycle` 转入 `instantiation`；模型等级只用于建议是否换用更强模型，不是进入路线的票据。
- 用户选中或基本确定第一项真实任务后，不要在模板态直接索取当天金额、真实文件或开始执行。先重新执行 `core/guides/first-use-execution-gates.md` 的“B. 首项任务开始前的实例化交接门”：取得正式方向和完整预览确认，再完成核心创建；模型等级是复杂任务建议，不是普通首次创建的硬门。已取得的信息不得让用户重答。
- 进入 `instantiation` 后，先读 `core/guides/first-use-execution-gates.md`，再按其中指向的实例化指南完成渐进访谈。写入前重新执行“C. 实例化写入门”：把语义保存为临时 JSON 文件，先让 `dashboard/scripts/first-instantiation-transaction.mjs` 只读检查并原样展示其 `user_preview`，得到确认后复用同一请求原子写入 manifest、正式档案和初始任务路线。胶囊、看板及可选登记表按需刷新；局部失败要报告但不能让已创建助手不可用。不得由模型逐文件手写正式状态、预造资产或临时发明替代脚本。

如果当前已经是实例：

- 不重复实例化；依据用户任务走渐进路由。
- 用户可用不精确的日常说法召回旧做法；先走 `domain-work` 的小地图规则，只比较少量低敏元数据，宽泛词或多候选不得直接决定沿用，命中并核对状态后才读有界正文。
- 用户只要求改变解释深度或提问节奏时，走 `guidance-mode` 路线，只修改交流方式，不改变锁定方向和既有资产。
- 若用户要求跨出锁定领域，说明应创建新实例，不修改本实例身份。
- 若当前会话尚未对这个实例形成有效接入回执，先从 `local-operations` 选择短路线 `host-session-resume`；它只在命中后读取极小宿主注册表和一个候选档案。只有首次接入、用户明确更换／恢复宿主、匹配不明或相关能力变化时，才展开 `host-integration` 完整协议。同一会话已验证时不重复；不得扫描全部宿主档案。
- 长期对话在每个新的实质用户目标开始前，只在模型上下文外重新运行一次严格启动入口，把 AI Carry 产品版本、实例 ID 和 manifest 摘要与本次会话启动基线比较；同一目标的连续回复不重复，完全一致时不加载升级正文。产品文件在本次运行中完成切换、上述比较不一致，或一个新运行明确接续尚未完成的升级时，从 `local-operations` 选择 `upgrade-session-activation`。当前对话优先在原子操作结束后的安全节点只加载目标版本的最小入口和相关变化、检查不可变宿主规则冲突，并自动验收一项无破坏的代表行为；不重新实例化、不清空任务、不修改用户资产，也不要求用户亲自测试。普通文件重读不能冒充宿主热加载；完整有界接续可以证明当前对话采用了可重载的新版产品规则，但不声称宿主内部提示词已被改写。宿主确实阻止原地接续时保留有效实例与可用能力，新任务只作为用户希望立即采用受影响行为时的最后兼容路线。

控制记录与唤醒胶囊修订不一致、存在待处理操作或无法解析时，不能把未知状态当作空态；将跨会话提醒标为局部降级并路由到定向恢复，不提前加载无关记忆。普通对话、自然语言召回、学习和不依赖提醒的任务继续。当前时间达到 `next_wakeup_at` 时才读取非启动时间索引，先给一个定时项数量摘要；用户选中具体项目后再读正文。

联网或读取任何外部内容前，必须先路由到紧凑安全边界；普通离线任务不加载完整安全协议。

任务进行中只在真实语义事件发生时通过根地图判断相应规则，例如下一项实质行动、已验证状态变化、失败／冲突、到期信号、任务结果、正式资产变化，或有价值小阶段／任务结束时的学习。这些事件只向现有有界路由补充少量当前工作线索，不建立新的启动层、不扫描全部资产，也不把工作线索当成授权；用户当次否定和纠正优先。命中学习保存时按路线使用 `dashboard/scripts/learning-save-cli.mjs` 的简短请求入口，不让模型手写正式资产 ID、路径、TOML、时间、哈希、成熟度或底层事务回执；单项保存失败只影响这一项并用自然语言说明，普通对话与其他能力继续。准备新增、修改、删除、重命名、移动、迁移、升级或发布 AI Carry 正式内容时，才按 `assistant.toml` 的 `durable_change_protocol` 加载完整修改质量原则；只读任务、普通使用和临时草稿不加载。新增具体触发规则不得继续扩写本启动文件；应登记到非启动触发注册表和所属领域。
