# Agent Carry 启动

启动阶段只做以下事情，不提前读取完整协议、SOP、记忆或能力正文。

1. 读取 `assistant.toml`，确认产品版本和启动预算。
2. 读取 `instance/manifest.toml`，确认当前是模板还是实例、实例方向是否锁定，以及极小的交流、学习与隐私处理模式。
3. 启动只读取 `[signals].startup_reads` 明确列出的 `instance/signals/control.toml` 与 `instance/maps/signal-map.toml`。`[signals].projections` 是更新事务必须共同维护的完整输出集合，不是启动读取清单；启动不得因为看到 `time_projection`、`source_root`、`registry`、`protocol`、`schema` 或 `template` 字段就打开对应文件或枚举目录。此时只判断：事务是否干净、是否有待处理短路线、当前时间是否达到唤醒胶囊中唯一的 `next_wakeup_at`。日期未到时不得读取 `instance/maps/time-trigger-map.toml`；普通观察状态、全部日程、指标、状态卡和规则正文都不在启动时扫描。
4. 读取 `core/maps/root-map.toml`，为当前请求选择一个根分类；只有确有组合需要时才选择少量多个分类。
5. 再读取所选分类的小地图，并只打开匹配目标。

如果 `instance/manifest.toml` 的 `state = "template"`：

- 立即从 `domain-lifecycle` 选择 Level 1 可执行的 `first-run-guidance`，读取 `core/guides/instantiation-guide.md` 的“安装成功后的第一条回复”与当前所需段落。不得用安装报告、版本／路径清单或一句没有解释的交流方式问题结束对话，也不得因为正式实例化需要 Level 3 而跳过欢迎和首次选择。
- 第一条面向用户的话必须先邀请创建自己的助手；当前任务包含安装时，同时说明看板是否已经在用户可见窗口中打开、怎样从首页右侧的“第一次使用”卡片进入“创建我的助手”，以及不看或打不开看板时可以直接在当前聊天继续。
- 在同一条回复中完整解释三种引导路线适合谁、当前 Agent 会怎样配合，再只询问这一个选择；用户不知道时允许直接说职业、困难和目标，由 Agent 帮助判断。三种路线只是可调整的交流方式，不是能力等级。
- 安装入口验证已经结束、准备发送这条回复时，重新读取 `core/guides/first-use-execution-gates.md` 的“A. 安装完成后的回复门”；不能因为较早读过实例化指南就跳过临门检查。
- 用户回答后，再询问选择“通用个人助手”“具体领域专属助手”还是“先帮我判断”。说明专业工作通常更适合领域专属助手；“先帮我判断”不是第三种正式方向；方向一旦实例化便永久锁定，仍可复制模板创建其他实例。
- 看板生成的请求已经带有合法选择时不要重复询问；其中的“生成创建指令”只是在组织请求，不代表已经实例化。保留已有选择，并在正式访谈前从 `domain-lifecycle` 转入 Level 3 的 `instantiation`。
- 用户选中或基本确定第一项真实任务后，不要在模板态直接索取当天金额、真实文件或开始执行。先重新执行 `core/guides/first-use-execution-gates.md` 的“B. 首项任务开始前的实例化交接门”：取得正式方向，按模型等级协议等待用户明确确认当前模型处于 Level 3；当前不足时再请用户手动切换。之后才完成实例化；已取得的信息完整交接，不让用户重答。
- 进入 `instantiation` 后，先读 `core/guides/first-use-execution-gates.md`，再按其中指向的实例化指南完成渐进访谈、完整预览和确认。写入前重新执行“C. 实例化写入门”：只写实例身份、初始任务族、治理排期、当前宿主最小档案、空信号和正式来源快照，不预先制造记忆、能力、SOP、经验、学习建议或普通待办。

如果当前已经是实例：

- 不重复实例化；依据用户任务走渐进路由。
- 用户只要求改变解释深度或提问节奏时，走 `guidance-mode` 路线，只修改交流方式，不改变锁定方向和既有资产。
- 若用户要求跨出锁定领域，说明应创建新实例，不修改本实例身份。
- 若当前会话尚未对这个实例形成有效接入回执，先从 `local-operations` 选择短路线 `host-session-resume`；它只在命中后读取极小宿主注册表和一个候选档案。只有首次接入、用户明确更换／恢复宿主、匹配不明或相关能力变化时，才展开 `host-integration` 完整协议。同一会话已验证时不重复；不得扫描全部宿主档案。

控制记录与唤醒胶囊修订不一致、存在待处理操作或无法解析时，不能把未知状态当作空态；只路由到统一触发恢复协议，不提前加载无关记忆。当前时间达到 `next_wakeup_at` 时才读取非启动时间索引，先给一个定时项数量摘要；用户选中具体项目后再读正文。

联网或读取任何外部内容前，必须先路由到紧凑安全边界；普通离线任务不加载完整安全协议。

任务进行中只在真实语义事件发生时通过根地图判断相应规则，例如动作门禁、正式资产变化、失败／冲突、任务结束学习或环境变化。准备新增、修改、删除、重命名、移动、迁移、升级或发布 Agent Carry 正式内容时，才按 `assistant.toml` 的 `durable_change_protocol` 加载完整修改质量原则；只读任务、普通使用和临时草稿不加载。新增具体触发规则不得继续扩写本启动文件；应登记到非启动触发注册表和所属领域。
