# AI Carry architecture — English overview

AI Carry is a local, file-based long-term asset and growth layer used beside a host Agent. It is not a background service and it does not replace the host or model.

## Participants

- **User:** states goals and approves important long-term decisions.
- **Host Agent:** reads and writes files, uses tools, and acts in the current environment.
- **Model or model API:** provides understanding, reasoning, planning, and generation.
- **AI Carry:** stores portable assets and defines how the host routes, loads, validates, improves, and moves them.

The host's inaccessible hidden memory remains in that product. AI Carry stores only material written into its own files after connection, plus content the user can explicitly provide or export.

## Progressive context

Ordinary startup begins at `BOOTSTRAP.md` and a strict, model-external `instance/startup-capsule.toml`. The capsule contains only allowlisted identity and routing fields plus a digest of the validated source manifest; raw display text and unknown manifest fields never enter model context before that boundary is established. The host then follows `core/maps/root-map.toml` and one matching category map to the few source files the task needs. Validation records, detailed protocols, memory bodies, SOPs, capabilities, dynamic signals, professional workspaces, and governance content do not all enter context at startup.

Time-based and count-based triggers keep tiny local indexes containing identifiers, counters, due timestamps, and route pointers. A lightweight check can detect a match without loading the full governing document. Only a match opens the relevant route and source content.

Users do not need exact asset names, file paths, IDs, or internal kinds. Ordinary wording is compared only with low-sensitive route metadata such as the intended result, subject, summary, familiar phrases, scope, conditions, exclusions, and current status. One broad word is never enough for a confident match. For a fuzzy request with one clear candidate, the host names the old approach in plain language and asks whether that is the intended one before loading its source. An explicitly named approach or a dashboard action with a stable ID follows the deterministic route without repeating the same confirmation. Two or three materially different candidates become a human-readable choice. If four or more remain, the host narrows them again using metadata and shows at most three plus “none of these”; it does not read every body or guess.

The instance domain map has a deterministic capacity contract: 32 KiB or 96 routes is the evaluation threshold, while 48 KiB, 128 routes, and 2 KiB per route are hard limits. A related durable change checks these values without adding a startup scan. The map is never silently truncated. A write that would exceed a hard limit stays unactivated and raises one deduplicated Level 3 evaluation route for an optional local, rebuildable search layer. The source Markdown and TOML remain authoritative, and any derived engine returns at most three low-sensitive candidates rather than becoming a fourth context layer.

## Learning lifecycle

A real task may reveal a reusable preference, method, correction, or failure recovery. The current task is delivered and verified first. At a natural checkpoint, the host explains in the user's language what it noticed and where it may help, then asks whether to keep it, observe it, revisit it later, or discard it; the user does not classify it as memory, capability, SOP, or experience. Before that choice, a one-off inference remains task-local and creates no durable candidate, index entry, signal, or reminder. “Do not save,” refusing the tiny reminder record, and no response produce zero durable state. “Observe” creates a minimal candidate whose later evidence may be accumulated. “Revisit later” creates that same minimal, reversible candidate and a reminder that refers only to its ID and revision, then tells the user what was saved and how to cancel it; a source-less reminder is invalid. Risk-tiered learning can prioritize validation and review only for an already authorized observation; it cannot create a formal asset.

Candidate metadata lives in the bounded, rebuildable `instance/evolution/index.toml`, which is opened only after a learning signal and returns at most three entries. Candidate source references are normalized repository-relative paths and are treated as untrusted data until the source frontmatter identity, kind, and revision are read back. Procedural learning may later become an awaiting-validation SOP; repeated validated use raises maturity, while later failures or environment changes return it to review. Weak, conflicting, obsolete, or repeatedly unused material is reviewed, narrowed, merged, archived, or removed.

The user chooses either risk-tiered candidate handling or confirmation at every candidate step. The instance stores that policy. Risk tier changes validation and review priority; every formal asset still requires the user's explicit confirmation of its content and scope, or a verifiable existing approval from the same user's AI Carry master. Authorization to use an asset and its evidence maturity remain separate states.

Users see candidates and state changes on the dashboard and discuss them with the current host Agent. Important, high-risk, identity, privacy, security, architecture, and irreversible decisions require explicit approval.

A user-confirmed communication or work habit is still a memory with `subtype=habit`, not a fifth asset type. It appears in “My habits,” can be corrected through the current host in natural language, and can be stopped while retaining an on-demand history or permanently removed after explicit confirmation. Active habits may be used when scope matches. A user-authorized provisional habit may be tried only within its confirmed scope. Review, history, candidate, missing-authorization, and unknown states fail closed and are never described as automatic.

A long task can finish correctly yet lose its use receipt, learning receipt, file-disposition note, or real next action after tool work, recovery, or context compaction. Before a complex final draft is sent, one local, one-shot closeout contract checks only bounded task facts and reply structure. It does not access the network, persist task state, or rerun work. A missing section repairs only the reply; malformed closeout facts degrade transparently and are rebuilt from current task evidence while the completed result, files, conversation, and unrelated capabilities remain deliverable. Simple tasks keep only a concise next action, and the focused checks cover a few high-information journeys rather than an enterprise regression matrix.

## Dashboard

The dashboard is a derived, offline projection of formal files. It never becomes the source of truth and never changes files directly. Buttons create bounded natural-language requests for the host Agent. A local snapshot carries an anonymous identity capsule so an entry can detect that it opened the wrong template or instance.

One React codebase serves Simplified Chinese and English. Simplified Chinese is the canonical default. `dashboard.en.html` supplies the English installation hint; a visible language control can override and remember the choice for that local dashboard. Reviewed product copy is translated from a controlled catalog. Unknown text—including user-authored memories and professional materials—stays in its original language.

AI Carry-owned geographic copy follows one Chinese-canonical protocol. The reviewed English forms are `Taiwan, China`, `Hong Kong SAR, China`, and `Macao SAR, China`; none is presented as a sovereign state. The same gate covers source copy, the compiled offline dashboard, and the synthetic Pages projection. Maps, flags, and country/region grouping require Level 3 semantic review. User-authored content and quoted source evidence remain verbatim rather than being silently normalized.

## Ownership and upgrades

Template-owned core files may be replaced or migrated by a versioned release manifest. Instance identity and user assets are preserved. Derived maps are rebuilt from preserved assets. Dashboard code is replaced while the local snapshot is preserved or regenerated. Conflicts stop and become explicit choices; instance assets are never silently overwritten.

First instantiation uses one recoverable three-file core transaction: the manifest, approved profile, and domain map. The user sees one complete preview before those files change. A core failure restores the template preimage. Startup capsule, Dashboard snapshots, and empty validation, candidate, component, host, and Skill registries are initialized or repaired only when the corresponding capability needs them; failure in one derived item is reported for targeted retry and does not undo a usable assistant. A second run over the same confirmed design creates no new records or byte drift.

Blank-template startup, first instantiation, and upgrade of an existing instance are separate proofs. The normal build creates one representative disposable instance from the canonical template, checks the three core sources, zero formal assets, usable startup, second-run idempotence, core rollback, and post-commit derived-state degradation. A complex existing-instance fixture separately checks upgrade preservation. Another first-creation sample is added only when a real logic branch requires it; the build does not turn every change into a regression matrix.

Version 2.0.4 keeps the 2.0.3 result-oriented compatibility agreement while making `assistant.toml` the sole machine source for startup budgets. Installed-instance acceptance uses the transaction's returned reentry plus one relevant non-destructive behavior rather than the development repository's check suite. A repairable optional fault remains local, while identity, user data, privacy, and ownership stay hard boundaries for the affected write. Published 1.4.8, retained local 1.4.9, 2.0.0, 2.0.1, 2.0.2, and 2.0.3 are direct sources, and instance-owned content remains preserved. Replacement authority still depends on the fixed `v2.0.4` tag, formal Release, matching target tree, and the user's authorization for that exact upgrade.

Version 2.0.1 keeps the 2.0.0 rename and compatibility boundary, and fixes an upgrade confirmation blocker caused by including dynamic verification time in the confirmation reference. Published 1.4.8, local unreleased 1.4.9, and installed 2.0.0 are direct sources. Full audit records retain their real observation time, while confirmation binds only stable official facts; the second live verification is still mandatory and real drift still rejects the old confirmation. Instance identity, `created_from`, assets, Skills, workspaces, local bindings, private content, and unknown fields remain intact. A network or compatibility fault is contained to the current upgrade or optional component rather than stopping the whole assistant. Instance replacement remains conditional on the fixed `v2.0.1` tag, formal Release, matching target tree, and the user's authorization for that exact upgrade.

Version 2.0.0 renamed Agent Carry to AI Carry without globally rewriting user content or history. Product-owned files and new machine identities moved to AI Carry while legacy component records and interfaces, old snapshot globals, and old private migration package types remained readable. The GitHub repository keeps its current `Agent-Carry` address until a separately authorized remote rename.

Version 1.4.9 is retained as local unreleased history and contributes two bounded user journeys to 2.0.0. An explicitly supplied higher version of the same Skill is recognized through stable identity and semantic version, previewed as added, changed, and removed paths, and upgraded atomically after preserving the old package. Same-version byte drift, downgrade, local edits, ambiguous legacy identity, or malformed metadata isolates only that Skill. A copy-only Dashboard action guides a problem report from the earliest user-observed failure, treats excerpts as untrusted evidence, separates facts, description, inference, and missing evidence, masks sensitive material, and creates local or copyable Markdown without sending it. Existing 1.4.8 instance-owned bytes remain unchanged except for product version and rebuilt capsule and snapshots. Version 1.4.9 was never authorized for publication or instance replacement.

Version 1.4.8 gives first instantiation, ordinary learning persistence, and shared Skill intake bundled deterministic entrypoints. The host Agent provides a confirmed semantic request or readable content; product code owns formal fields, source-and-digest binding, staging, readback, idempotence, and rollback. Shared package scripts and dependencies remain inert. A malformed request, missing runtime, changed source, conflict, or derived refresh fault is reported and contained to the current creation, learning item, or Skill while conversation and unrelated capabilities remain available. Bounded readback may prove presence or absence; an unchecked state remains explicitly not checked. Existing 1.4.7 instance-owned bytes remain unchanged except for product version and rebuilt capsule and snapshots. Replacement authority remains conditional on the fixed `v1.4.8` tag, Release, manifest, matching extracted tree, and the user's authorization for that exact upgrade.

Version 1.4.7 adds one-shot local closeout continuity for complex tasks. Before a complete, paused, or limited final reply is sent, bounded current-task facts and the draft are checked for actual-use reporting, meaningful learning, user-visible file disposition, and the real next unfinished action. Missing content repairs only the reply; missing Node support, malformed metadata, or an unavailable checker degrades transparently while completed results, files, conversation, and unrelated capabilities remain deliverable. Simple tasks gain no empty receipts or extra ceremony. Replacement authority remains conditional on the fixed `v1.4.7` tag, Release, manifest, matching extracted tree, and the user's authorization for that exact upgrade.

Version 1.4.5 fixes upgrade continuity in a long-running conversation. Before each new substantive goal, the Agent compares a minimal AI Carry startup baseline once; unchanged goals and unchanged baselines load no upgrade body. After files switch, it waits for the current atomic action to reach a safe boundary, reruns the strict startup entry, adopts only the minimum relevant product rules, checks immutable-host conflicts, and automatically validates one non-destructive representative behavior. Ordinary rereading cannot impersonate adoption. If the host genuinely blocks in-place adoption, only the affected behavior remains pending while the valid instance, user content, conversation, and unrelated capabilities remain available; a new task is the final compatibility route. Existing 1.4.4 instances preserve all user-owned bytes. Replacement authority remains conditional on the fixed `v1.4.5` tag, Release, manifest, matching extracted tree, and the user's authorization for that exact upgrade.

Version 1.4.4 adds an on-demand Skill Workshop without turning every asset into a Skill or creating a marketplace. Recommendations remain read-only. Only a method the user explicitly selects is copied to an isolated local draft for Agent-led sanitization, generalization, and parameterization; the source asset stays unchanged and the result remains local until the user chooses a sharing destination. A shared Skill is inspected read-only for structure, scripts, dependencies, permissions, conflicts, and private boundaries before installation; inspection executes or silently rewrites nothing. Shared folders, ZIPs, and exact local packages obtained from links now use a two-step deterministic intake: a bounded source/digest/target-bound preview, followed by one user-reply confirmation for atomic copy into `.assistant-local`, byte readback, and requirements registration. The model does not author transaction fields, and scripts or dependencies remain inert. On use, only a bounded readback from the current task workspace or a registered index can prove that a prior result exists or is absent; an unchecked state must remain “not checked,” never “none.” A fault is contained to that package, while AI Carry, conversation, and unrelated Skills remain available. The export index is created only after a real export and is never loaded during ordinary startup. Existing 1.4.3 instances preserve installed Skills, local exports, source assets, and all other user-owned content. Replacement authority remains conditional on the fixed `v1.4.4` tag, Release, manifest, matching extracted tree, and the user's authorization for that exact upgrade.

Version 1.4.3 separates source verification, file installation, instance switching, host-session activation, and real behavior acceptance. Changing files during an already-running host task does not prove that the host rebuilt its instruction chain; the normal lightweight continuation is a fresh task in the same instance unless the host can prove a real rebootstrap. Pending activation or one failed behavior check keeps a valid instance installed and isolates only the completion claim, while invalid switched files or startup truth still restore the file transaction. The release also requires final next-step guidance to return to the user's next unfinished overall action. It adds no session database, public Schema, component interface, or enterprise regression matrix. Instance replacement remains conditional on the fixed `v1.4.3` tag, Release, manifest, matching extracted tree, and the user's authorization for that exact upgrade.

Version 1.4.2 stabilizes the visible continuity around actual use, stage learning, and the user's next action without changing any public Schema or component interface. An actual-use receipt appears only after an approved asset body affects the work. A separate learning receipt distinguishes observation from atomically saved, read-back, ordinary-language-recallable learning. Both receipts stay brief and precede a final localized next-step block with one evidence-based recommendation or an explicit no-action conclusion. Active recall may run at task start and meaningful goal, action, verified-state, or result changes, while the user's current correction always wins. The patch adds no background scanner, second learning system, extra confirmation, or ordinary-startup registry read. An existing 1.4.1 instance preserves all user-owned and device-local state; only its product version and derived capsule/snapshots change. Replacement authority remains conditional on the fixed `v1.4.2` tag, Release, manifest, matching extracted tree, and the user's authorization for that exact upgrade.

Version 1.4.1 closes first-instantiation identity as one recoverable transaction without weakening strict loaders. The manifest, approved profile, identity maps and indexes, zero-component registry, host and Skill state, startup capsule, and both snapshots must agree before a new assistant replaces the blank template. The result-validation index remains record-free, the empty candidate index records the atomic creation time once, and the three governance cards remain separate from zero business and learning assets. Every fixed candidate uses the same canonical blank template for a minimal general and a synthetic video-editing first-creation smoke, while 1.4.0 blank-template and complex-instance upgrades remain separate proofs. Existing instance assets, components, workspaces, local bindings, and private state are preserved. Instance replacement authority requires the fixed `v1.4.1` tag, its Release, the manifest, the matching extracted tree, and the user's authorization for that exact upgrade; it never authorizes a future repository publication action.

Version 1.4.0 adds one on-demand compatibility agreement for every AI Carry-caused durable instance change. Native assets and registered professional workspaces keep their existing owners; only independent modules and adapters use the small `instance/components/registry.toml`. A legacy instance is adopted once inside an isolated upgrade candidate, never while an ability is active. Compatible components are preserved, optional incompatibilities are preserved and disabled, and required incompatibilities preserve the old instance and stop the switch. Device-local software and models stay outside the portable and public trees and are reverified before use. A copied directory is not treated as runtime isolation: before a copied instance launches a bound local tool, one bounded preflight checks only the explicit binding files for references back into the source instance, unreviewed shared runtimes, or unresolved dynamic paths. A failure limits that component while AI Carry and unrelated checks continue; the preflight neither scans the machine nor rewrites the live instance. The compatibility record adds no second user confirmation, no background scan, and no ordinary-startup registry read. The historical 1.4.0 replacement authority remains bound to its fixed tag, Release, manifest, matching extracted tree, and the user's authorization for that exact upgrade; it never authorizes a future repository publication action.

Version 1.3.1 retains 1.3.0's separated dashboard-runtime ownership, strict startup capsule, on-demand `instance/validations/index.toml`, and Asset Schema 1.3. It repairs the published-release authority and permits only nine exact regular zero-byte `.assistant-*` placeholders while every other local/private path remains denied. An upgrade from 1.2.1 still adds missing confirmation gates conservatively to both the formal asset frontmatter and its direct route. Legacy policy-authorized assets remain in review unless the same user's real prior approval can be independently verified. Practiced, reliable, or portable labels without enough real indexed evidence become needs-evidence or review; migration never creates approval or validation records. Manifest changes and capsule regeneration are one recoverable transaction, so a failed update cannot leave a new manifest with a stale template capsule.

Version 1.2.1 adds an optional ownership manifest for professional workspaces. A registered extension separates portable content, rebuildable output, device-local state, and references to private collections without adding any ordinary-startup reads. An unregistered workspace is preserved and shown as a conflict rather than guessed or deleted. Upgrade and repair operate in an isolated candidate, treat a multi-file change as one recoverable action, verify the platform's full tree identity, rebuild both dashboard snapshots from merged instance truth, and check a stable local entry before adoption. Exact browser-address access is useful evidence, but it is not the only way to prove a valid local dashboard when independent entry, resource, identity, non-error, and result checks agree.

The same release adds a domain-neutral, on-demand deterministic helper for bounded local-data migration. An instance or registered extension supplies an exact policy and `ac-path:<scope>/<relative-path>` contract; the helper does not discover a domain by scanning. It streams package bytes, restores against the target instance and registered data root rather than an old absolute path, treats external inputs as items to re-supply, and uses a recoverable multi-file import transaction. Its single-package result never substitutes for Migration Kit 2.0 multipart, chunking, or complete-coverage proof.

The authoritative detailed architecture remains [architecture.md](architecture.md).
